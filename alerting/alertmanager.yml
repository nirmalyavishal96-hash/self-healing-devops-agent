route:
  receiver: "default"
  group_wait: 1s
  group_interval: 5s
  repeat_interval: 5s

receivers:
  - name: "default"
    webhook_configs:
      - url: "http://healer:5001/webhook"
        send_resolved: true