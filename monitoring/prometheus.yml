global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alert_rules.yml   

scrape_configs:
  - job_name: 'self_healing_app'
    static_configs:
      - targets: ['self_healing_app:5000']

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "alertmanager:9093"